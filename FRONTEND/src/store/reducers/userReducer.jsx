import {createSlice} from "@reduxjs/toolkit";
import {  } from "react";
const initialState={
    data:[]
}

const userSlice=createSlice({
    name:'user',
    initialState,
    reducers:{

    },
})
export default userSlice.reducer